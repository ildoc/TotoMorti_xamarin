<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resx="clr-namespace:TotoMorti.Resx;assembly=TotoMorti"
             x:Class="TotoMorti.Pages.MainPage"
             Padding="10,20,10,10"
             Title="TotoMorti">
    <ContentPage.ToolbarItems>
        <!--<ToolbarItem Text="?" Command="{Binding ShowAboutCommand}" />-->
        <ToolbarItem Text="?" Clicked="ShowAbout" />
    </ContentPage.ToolbarItems>
    <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="Fill">
        <Button Text="{x:Static resx:AppResources.TotoLists}" Command="{Binding TotoListListCommand}" />
        <Button Text="{x:Static resx:AppResources.Groups}" Command="{Binding GroupsCommand}" IsEnabled="False" />
        <!--<Button Text="{x:Static resx:AppResources.Sessions}" Command="{Binding SessionCommand}" />-->
        <Button Text="{x:Static resx:AppResources.CelebrityList}" Command="{Binding CelebrityListCommand}" />
    </StackLayout>
</ContentPage>