<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             x:Class="TotoMorti.Views.AvailableCelebrityListView"
             mvvm:ViewModelLocator.AutowireViewModel="True"
             Padding="10,20,10,10">

        <StackLayout VerticalOptions="Fill">
            <ListView x:Name="List" ItemsSource="{Binding AvailableCelebrities}" HasUnevenRows="True"
                    SelectedItem="{Binding SelectedCelebrity}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" Padding="5">
                                <Image Source="{Binding ImageUrl}" />
                                <StackLayout HorizontalOptions="StartAndExpand">
                                    <Label Text="{Binding FullName}" />
                                    <Label Text="{Binding Age}" TextColor="Gray" />
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
</ContentPage>
