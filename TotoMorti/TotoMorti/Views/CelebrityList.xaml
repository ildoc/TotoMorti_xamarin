<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="TotoMorti.Views.CelebrityListView">

  <ListView x:Name="List" ItemsSource="{Binding CelebrityList}" HasUnevenRows="True"
            ItemSelected="">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout Orientation="Horizontal" Padding="5">
            <Image Source="{Binding ImageUrl}" />
            <StackLayout HorizontalOptions="StartAndExpand">
              <Label Text="{Binding FullName}" />
              <Label Text="Stato" TextColor="Gray" />
            </StackLayout>
            <!--<Button Text="Edit Celebrity" Command="{Binding BindingContext.EditCelebrityCommand, Source={x:Reference ContentPage}}" CommandParameter="{Binding}" />-->
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>
